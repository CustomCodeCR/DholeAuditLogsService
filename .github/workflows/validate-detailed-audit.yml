name: Validate Detailed Audit

on:
  push:
    branches: [feature/detailed-audit-20260828]
  workflow_dispatch:

permissions:
  contents: write
  packages: read

jobs:
  validate:
    runs-on: [self-hosted, linux, x64, dhole]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Build and generate EF migration
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          docker run --rm \
            --user "$(id -u):$(id -g)" \
            -e HOME=/tmp \
            -e GITHUB_ACTOR="$GITHUB_ACTOR" \
            -e GITHUB_TOKEN="$GITHUB_TOKEN" \
            -v "$PWD:/src" \
            -w /src \
            mcr.microsoft.com/dotnet/sdk:10.0 \
            bash -lc '
              set -euo pipefail
              export NuGetPackageSourceCredentials_github="Username=${GITHUB_ACTOR};Password=${GITHUB_TOKEN};ValidAuthenticationTypes=Basic"
              dotnet restore src/Dhole.AuditLogs.Api/Dhole.AuditLogs.Api.csproj --configfile NuGet.Config
              dotnet build src/Dhole.AuditLogs.Api/Dhole.AuditLogs.Api.csproj --no-restore
              if ! ls src/Dhole.AuditLogs.Persistence/Migrations/*DetailedAuditContext*.cs >/dev/null 2>&1; then
                dotnet tool install --tool-path /tmp/dotnet-tools dotnet-ef --version 10.0.9
                /tmp/dotnet-tools/dotnet-ef migrations add DetailedAuditContext \
                  --project src/Dhole.AuditLogs.Persistence/Dhole.AuditLogs.Persistence.csproj \
                  --startup-project src/Dhole.AuditLogs.Api/Dhole.AuditLogs.Api.csproj \
                  --context ServiceDbContext \
                  --no-build
              fi
            '

      - name: Validate API and Worker images
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          docker build --pull \
            --build-arg GITHUB_ACTOR="$GITHUB_ACTOR" \
            --secret id=github_token,env=GITHUB_TOKEN \
            -f src/Dhole.AuditLogs.Api/Dockerfile \
            -t dhole/auditlogs-detailed-api-validation .
          docker build --pull \
            --build-arg GITHUB_ACTOR="$GITHUB_ACTOR" \
            --secret id=github_token,env=GITHUB_TOKEN \
            -f src/Dhole.AuditLogs.Workers/Dockerfile \
            -t dhole/auditlogs-detailed-worker-validation .

      - name: Commit generated migration
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/Dhole.AuditLogs.Persistence/Migrations
          if git diff --cached --quiet; then
            echo "No generated migration changes."
            exit 0
          fi
          git commit -m 'feat(audit): migrate detailed audit context'
          git push origin HEAD:feature/detailed-audit-20260828
